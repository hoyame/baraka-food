import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'BarakaFood - Salle',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
